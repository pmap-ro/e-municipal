image: Visual Studio 2017
init:
  - git config --global core.autocrlf true
install:
  - ECHO %APPVEYOR_BUILD_WORKER_IMAGE%
  - dotnet --version
  - dotnet restore
build_script:
  - dotnet build
after_build:
- cmd: dotnet publish src\Web\ --output publicar --configuration Release && 7z a src\Web\e-municipal.zip src\Web\publicar\**\*.* && dir src\Web
artifacts:
- path: src\Web\e-municipal.zip
  name: Publicar_WebDeploy
  type: Zip
